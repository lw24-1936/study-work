---
title: 软件源与镜像
created: 2026-08-18
updated: 2026-08-18
type: concept
tags: [linux, repository, mirror]
---

# 软件源与镜像

整理日期：2026-08-18

> 状态：已完成

## 目录

- [概述](#概述)
- [官方源与镜像源](#官方源与镜像源)
- [自建本地源](#自建本地源)
- [密钥与签名校验](#密钥与签名校验)
- [应用场景实战](#应用场景实战)
- [最佳实践与踩坑记录](#最佳实践与踩坑记录)
- [相关文档](#相关文档)

## 概述

软件源（repository）是「软件包存放在哪、从哪下载」的配置。官方源（在软件官方服务器）和镜像源（第三方镜像站，如阿里云/清华，内容一样但更快）是两种选择。生产环境常见需求：换镜像源加速下载、自建本地源（内网离线装包）、密钥签名校验（防止包被篡改）。理解软件源的配置和签名校验，才能安全、高效地装软件。

```text
软件源三件事：
1. 官方源 vs 镜像源 —— 内容一样，镜像源在国内更快
2. 自建本地源 —— 内网环境离线装包（本地目录或内网 HTTP 服务器）
3. 密钥签名 —— GPG 签名校验，确保包来源可信、未被篡改
```

## 官方源与镜像源

官方源是软件官方维护的源（下载可能慢），镜像源是第三方镜像站同步的副本（内容一样、国内访问快）。换镜像源的收益就是「下载速度」，尤其国内服务器——阿里云、清华、中科大镜像站是常用选择。Debian 系源在 /etc/apt/sources.list(.d/)，Red Hat 系在 /etc/yum.repos.d/*.repo。换源后必须刷新索引（apt update / dnf makecache）。

```text
# 实测（本机 Debian 系源配置）
$ ls /etc/apt/sources.list.d/
docker.list  tailscale.list  ubuntu.sources  ubuntu.sources.curtin.orig
# Ubuntu 24.04 用新的 deb822 格式 .sources 文件，第三方源（docker/tailscale）单独放
```

```text
Debian 系源格式（Ubuntu 24.04 deb822 格式）：
Types: deb
URIs: http://mirrors.aliyun.com/ubuntu/
Suites: noble noble-updates noble-security
Components: main universe restricted multiverse
```

```shell
# Debian 系换源（改源后 apt update）
sed -i 's/archive.ubuntu.com/mirrors.aliyun.com/g' /etc/apt/sources.list
apt update

# Red Hat 系换源（CentOS 换阿里云）
sed -i 's/mirror.centos.org/mirrors.aliyun.com/g' /etc/yum.repos.d/CentOS-*.repo
dnf makecache
```

## 自建本地源

自建本地源解决「内网/离线环境装包」——把包下载到本地，做成本地源，内网机器从本地源装（不依赖外网）。两种方式：本地目录源（file:// 或拷贝到本地路径）和局域网 HTTP 源（内网服务器开 HTTP 服务）。生产内网（无外网或有流量限制）常用。Debian 用 apt-mirror/debmirror 同步，Red Hat 用 reposync 同步。

```shell
# Debian 系：同步官方源到本地
apt-mirror                      # 配置 /etc/apt/mirror.list 后同步
# 内网机器指向本地目录源
# deb [trusted=yes] file:/var/spool/apt-mirror/mirror/archive.ubuntu.com/ubuntu noble main

# Red Hat 系：同步仓库到本地
reposync --repoid=baseos --download-metadata -p /var/www/repo
createrepo /var/www/repo/baseos
# 内网机器指向本地 HTTP 源
# baseurl=http://192.168.1.10/repo/baseos
```

## 密钥与签名校验

软件包的「密钥签名」防止包被篡改：官方用私钥给包签名，客户端用公钥验证，签名不符就拒绝安装。Debian 系用 apt-key/密钥环（/etc/apt/trusted.gpg.d/），Red Hat 系用 gpgkey（/etc/pki/rpm-gpg/）。添加第三方源时通常要导入它的公钥——这一步是信任的建立，别随意导入来路不明的密钥。

```shell
# Debian 系：添加第三方源 + 导入密钥
curl -fsSL https://example.com/key.gpg | gpg --dearmor -o /etc/apt/trusted.gpg.d/example.gpg
apt update

# Red Hat 系：导入 RPM-GPG 密钥
rpm --import /etc/pki/rpm-gpg/RPM-GPG-KEY-CentOS-8
rpm -qa gpg-pubkey          # 查看已导入的密钥
```

```text
签名校验原理：
1. 签名 —— 官方用私钥给包/索引签名
2. 验证 —— 客户端用公钥验证签名，不符拒绝
3. 导入密钥 = 建立信任 —— 只导可信来源的密钥，别乱导
```

## 应用场景实战

### 场景一：国内服务器换镜像源加速

```shell
# Ubuntu 换阿里云源
cp /etc/apt/sources.list /etc/apt/sources.list.bak
sed -i 's/archive.ubuntu.com/mirrors.aliyun.com/g' /etc/apt/sources.list
apt update
# 验证：apt update 应该明显变快
```

### 场景二：内网服务器自建本地源

```shell
# 1. 有外网的机器同步官方源
apt-mirror        # 配置 mirror.list 后同步到 /var/spool/apt-mirror
# 2. 内网机器配置指向本地目录
echo "deb [trusted=yes] file:/mnt/apt-mirror/mirror/archive.ubuntu.com/ubuntu noble main" > /etc/apt/sources.list
apt update
apt install nginx
```

## 最佳实践与踩坑记录

### 最佳实践

1. 国内服务器换阿里云/清华镜像源，下载速度差好几倍
2. 改源前备份原配置，改完必须 apt update / dnf makecache
3. 加第三方源务必导入对应密钥并确认来源可信，别乱导密钥
4. 内网/离线环境自建本地源（apt-mirror/reposync），别一台台手动拷包
5. 保持 gpgcheck=1 / 密钥校验开启，别为了省事关掉签名校验

### 踩坑记录

```text
坑 1：换源后没 update 就装包报错
结论：改了源直接 install，报 Unable to locate package。
原因：索引还是旧的，不知道新源有什么。
解法：换源后先 apt update（或 dnf makecache）刷新索引。

坑 2：镜像源和系统版本不匹配
结论：换了源后 apt update 报 404 或签名错误。
原因：源里的发行版本（如 focal/bionic）和系统版本（noble）不匹配。
解法：确认源的发行版本号和自己系统一致（lsb_release -cs）。

坑 3：加第三方源没导入密钥，报 NO_PUBKEY
结论：apt update 报 NO_PUBKEY 某某。
原因：第三方源有签名校验，但本机没导入它的公钥。
解法：按报错提示导入对应公钥（apt-key 或 trusted.gpg.d）。

坑 4：自建本地源缺了 Packages 索引文件
结论：本地源目录能访问，但 apt update 报找不到 Packages。
原因：同步时没生成/没同步索引文件（Packages.gz）。
解法：确认索引文件齐全，Red Hat 系要 createrepo 生成。

坑 5：为了省事关掉 gpgcheck 导致安全风险
结论：源里 gpgcheck=0 或 [trusted=yes]，包被篡改也照装。
原因：关闭签名校验，无法发现被替换的恶意包。
解法：保持签名校验开启，密钥正确导入即可。
```

## 相关文档

- [[09.1-Debian 系]] — apt 与源配合
- [[09.2-Red Hat 系]] — dnf 仓库管理
- [[09.3-源码编译]] — 源里没有时源码编译
- [[23.1-系统加固]] — 软件源与供应链安全
