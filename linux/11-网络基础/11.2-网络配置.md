---
title: 网络配置
created: 2026-08-18
updated: 2026-08-18
type: concept
tags: [linux, network, config]
---

# 网络配置

整理日期：2026-08-18

> 状态：已完成

## 目录

- [概述](#概述)
- [ip 与 ifconfig](#ip-与-ifconfig)
- [网卡配置](#网卡配置)
- [静态与 DHCP](#静态与-dhcp)
- [NetworkManager 与 systemd-networkd](#networkmanager-与-systemd-networkd)
- [应用场景实战](#应用场景实战)
- [最佳实践与踩坑记录](#最佳实践与踩坑记录)
- [相关文档](#相关文档)

## 概述

网络配置回答「网卡怎么配 IP、怎么上网」：用 ip 命令临时配（重启失效），或写网卡配置文件持久化（netplan/NetworkManager/systemd-networkd）。分静态 IP（手动指定）和 DHCP（自动获取）。现代 Linux 网络配置被 NetworkManager（桌面/通用）和 systemd-networkd（服务器）管理，Ubuntu 桌面用 netplan + NetworkManager。理解配置层级，才能「改了 IP 不生效」时知道去哪查。

```text
网络配置层级：
1. ip 命令 —— 临时配置（立即生效，重启失效）
2. 网卡配置文件 —— 持久化（netplan/NetworkManager/systemd-networkd）
3. 静态 IP —— 手动指定，服务器常用
4. DHCP —— 自动获取，客户端/临时环境常用
```

## ip 与 ifconfig

ip 命令是现代网络配置工具（iproute2 包），替代老的 ifconfig。ip 功能更全：ip addr（看/配 IP）、ip link（看/改网卡状态）、ip route（路由）。ifconfig 是老命令（net-tools 包），新系统默认不装。约定：新系统一律用 ip，ifconfig 只在老脚本里见到。ip 的配置是临时的，重启失效——持久化要写配置文件。

```text
# 实测（本机网卡）
$ ip -br link
lo               UNKNOWN  00:00:00:00:00:00   # 回环
enp59s0          DOWN     ...                  # 有线网卡（没插线）
wlp0s20f3        UP       ...                  # WiFi 网卡（在用）
docker0          UP       ...                  # Docker 网桥
tailscale0       UNKNOWN  ...                  # Tailscale VPN
# 网卡命名：en=有线、wl=无线，docker0/br-* 是 Docker 虚拟网卡
```

```shell
ip addr              # 看所有 IP
ip link              # 看网卡状态
ip link set wlp0s20f3 down   # 停用网卡
ip link set wlp0s20f3 up     # 启用网卡
ip -br addr          # 简洁版 IP 列表
```

## 网卡配置

网卡配置的持久化文件位置因发行版而异：Ubuntu 桌面用 netplan（/etc/netplan/*.yaml），Red Hat 系用 NetworkManager（/etc/NetworkManager/）或 /etc/sysconfig/network-scripts/（老 CentOS），服务器可用 systemd-networkd（/etc/systemd/network/*.network）。改完配置要重新应用（netplan apply / nmcli / systemctl restart）。网卡命名：en（以太网）、wl（无线）、docker0/br-*（虚拟）。

```text
# 实测（本机 netplan 配置）
$ ls /etc/netplan/
01-network-manager-all.yaml
90-NM-22291711-...yaml
# Ubuntu 用 netplan + NetworkManager 管理网络
```

```text
各发行版网卡配置位置：
Ubuntu（netplan）      /etc/netplan/*.yaml
Red Hat（NetworkManager） /etc/NetworkManager/ 或 /etc/sysconfig/network-scripts/
systemd-networkd       /etc/systemd/network/*.network
```

## 静态与 DHCP

静态 IP：手动指定 IP/掩码/网关/DNS，稳定（服务器、需要固定地址的服务用）。DHCP：自动从 DHCP 服务器获取（IP/网关/DNS 都自动），方便（客户端、临时环境用）。服务器一般配静态 IP（服务地址不能变），客户端/笔记本用 DHCP。配置静态 IP 要设四个东西：IP 地址、子网掩码、默认网关、DNS 服务器。

```yaml
# netplan 静态 IP 配置示例（/etc/netplan/01-static.yaml）
network:
  version: 2
  ethernets:
    enp59s0:
      dhcp4: no
      addresses: [192.168.1.100/24]
      routes:
        - to: default
          via: 192.168.1.1
      nameservers:
        addresses: [8.8.8.8, 114.114.114.114]
```

```shell
sudo netplan apply      # 应用 netplan 配置
```

## NetworkManager 与 systemd-networkd

两个网络管理守护进程：NetworkManager（功能全、自动管理，桌面和通用系统默认）和 systemd-networkd（轻量、声明式，服务器/容器默认）。nmcli/nmtui 管理 NetworkManager，systemd-networkd 用配置文件。Ubuntu 桌面用 NetworkManager（netplan 后端），服务器可选 systemd-networkd（更轻、更可控）。理解用哪个管理器，才能找到正确的配置入口。

```shell
nmcli device status          # NetworkManager 看设备
nmcli connection show        # 看连接配置
nmcli device wifi list       # 看 WiFi（NetworkManager）
systemctl status NetworkManager
```

```text
NetworkManager vs systemd-networkd：
1. NetworkManager —— 功能全、自动、GUI/桌面默认，nmcli 管理
2. systemd-networkd —— 轻量、声明式、服务器/容器默认，配置文件管理
```

## 应用场景实战

### 场景一：临时给网卡加一个 IP

```shell
# 1. 临时加 IP（重启失效）
ip addr add 192.168.1.200/24 dev enp59s0
# 2. 验证
ip addr show enp59s0
# 3. 临时删除
ip addr del 192.168.1.200/24 dev enp59s0
```

### 场景二：配置静态 IP（netplan，Ubuntu）

```shell
# 1. 写 netplan 配置
sudo vim /etc/netplan/01-static.yaml
# 2. 应用配置
sudo netplan apply
# 3. 验证
ip addr show && ip route show
```

## 最佳实践与踩坑记录

### 最佳实践

1. 服务器配静态 IP，客户端/临时环境用 DHCP
2. 用 ip 命令（现代），别用 ifconfig（已废弃）
3. 改配置后记得应用（netplan apply / nmcli / systemctl restart）
4. 远程改 IP 要小心——改错会断连，先确认再应用
5. 网卡配置位置因发行版而异，先确认用哪个管理器再改

### 踩坑记录

```text
坑 1：ip addr 配了 IP 重启就没了
结论：ip addr add 配的 IP，重启后消失。
原因：ip 命令是临时配置，不持久化。
解法：持久化要写 netplan/NetworkManager 配置文件。

坑 2：改 netplan 没 apply，配置不生效
结论：改了 netplan 文件，重启/查看还是老 IP。
原因：netplan 要 apply 才生效，没 apply 改了白改。
解法：sudo netplan apply，或重启网络服务。

坑 3：远程服务器改 IP 把自己改断连了
结论：改静态 IP 后 SSH 断了，连不上了。
原因：IP 改错或网关/掩码配错，网络不通。
解法：远程改 IP 用「临时加新 IP + 测试 + 删旧 IP」的方式，别直接改。

坑 4：ifconfig 找不到，脚本跑不了
结论：老脚本用 ifconfig，新系统报 command not found。
原因：ifconfig 在 net-tools 包，新系统不装。
解法：apt install net-tools，或把脚本改成 ip 命令。

坑 5：NetworkManager 和 systemd-networkd 同时管，配置冲突
结论：两个管理器都启动，网卡配置打架。
原因：两个守护进程同时管理网络，优先级混乱。
解法：只用一个管理器，另一个 disable 掉。
```

## 相关文档

- [[11.1-TCP-IP 协议栈]] — IP 地址与子网
- [[11.3-路由与转发]] — 网关与路由配置
- [[11.4-DNS 与主机名]] — DNS 与主机名
- [[11.5-网络诊断工具]] — 诊断网络配置问题
