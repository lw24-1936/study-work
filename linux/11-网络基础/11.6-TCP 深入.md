---
title: TCP 深入
created: 2026-08-18
updated: 2026-08-18
type: concept
tags: [linux, network, tcp]
---

# TCP 深入

整理日期：2026-08-18

> 状态：已完成

## 目录

- [概述](#概述)
- [三次握手](#三次握手)
- [四次挥手](#四次挥手)
- [状态机](#状态机)
- [TIME_WAIT](#time_wait)
- [Keepalive](#keepalive)
- [拥塞控制](#拥塞控制)
- [应用场景实战](#应用场景实战)
- [最佳实践与踩坑记录](#最佳实践与踩坑记录)
- [相关文档](#相关文档)

## 概述

TCP 是可靠的传输层协议，核心机制：三次握手（建立连接）、四次挥手（关闭连接）、状态机（连接的生命周期状态）、TIME_WAIT（主动关闭方等待）、Keepalive（探测死连接）、拥塞控制（防止网络拥塞）。理解这些，才能看懂 `ss -tan` 里的各种状态、排查「连接堆积」「端口耗尽」「TIME_WAIT 过多」等线上问题。

```text
TCP 六要素：
1. 三次握手 —— SYN → SYN+ACK → ACK，建立连接
2. 四次挥手 —— FIN → ACK → FIN → ACK，关闭连接
3. 状态机 —— LISTEN/ESTABLISHED/TIME_WAIT 等状态
4. TIME_WAIT —— 主动关闭方等待 2MSL，防止残留包
5. Keepalive —— 定时探测，检测死连接
6. 拥塞控制 —— 慢启动/拥塞避免，防止网络拥塞
```

## 三次握手

三次握手建立连接：客户端发 SYN → 服务端回 SYN+ACK → 客户端发 ACK，连接建立（ESTABLISHED）。为什么三次？因为要「双方都确认对方的收发能力」——两次不够（服务端不确定客户端能收到），三次正好。握手失败（如 SYN 发出去没 ACK）就是「SYN_SENT」状态，大量 SYN_SENT 说明连不上目标。

```text
三次握手：
客户端                      服务端
  --- SYN (seq=x) ----------->
  <-- SYN+ACK (seq=y, ack=x+1) ---
  --- ACK (ack=y+1) --------->
  连接建立（双方进入 ESTABLISHED）
```

## 四次挥手

四次挥手关闭连接：主动方发 FIN → 被动方回 ACK → 被动方发 FIN → 主动方回 ACK，连接关闭。为什么四次？因为 TCP 是「全双工」——双方各有一个方向要关闭，所以 FIN 和 ACK 分开（比握手多一次）。主动关闭方最后进入 TIME_WAIT 状态，等待 2MSL 后才完全关闭。

```text
四次挥手：
主动方                      被动方
  --- FIN ------------>
  <-- ACK -------------
  <-- FIN -------------
  --- ACK ------------>
  主动方进入 TIME_WAIT，等 2MSL 后关闭
```

## 状态机

TCP 连接有一系列状态（`ss -tan` 的 State 列）：LISTEN（监听）、SYN_SENT（发了 SYN 等 ACK）、SYN_RECV（收到 SYN 回了 ACK 等确认）、ESTABLISHED（已建立）、FIN_WAIT（关闭中）、CLOSE_WAIT（收到 FIN 但自己没关）、TIME_WAIT（主动关闭等待）、CLOSED（关闭）。排查连接问题就是看「哪些状态堆积」——CLOSE_WAIT 堆积说明服务没正确关闭连接（代码 bug），TIME_WAIT 过多说明短连接频繁。

```text
# 实测（本机 TCP 连接状态统计）
$ ss -tan | awk 'NR>1{print $1}' | sort | uniq -c
  2 CLOSE-WAIT
 23 ESTAB
  1 LAST-ACK
 53 LISTEN
 12 SYN-SENT
  1 TIME-WAIT
# 53 个 LISTEN（监听端口多），23 个 ESTAB（正常连接），12 个 SYN-SENT 值得关注
```

```text
关键状态含义：
LISTEN     监听中，等连接
ESTAB      已建立（正常通信）
SYN_SENT   发了 SYN 等对方 ACK（连不上时会堆积）
CLOSE_WAIT 收到 FIN 但自己没关（服务代码没 close，会泄漏）
TIME_WAIT  主动关闭方等待 2MSL（短连接多时会堆积）
```

## TIME_WAIT

TIME_WAIT 是「主动关闭方」在关闭后等待 2MSL（约 60 秒）的状态——作用：确保最后一个 ACK 到达、让残留的旧包消亡（防止旧包串到新连接）。TIME_WAIT 是正常的、必要的，但「短连接频繁」（如高频 HTTP 请求）时会产生大量 TIME_WAIT，耗尽端口（默认约 28 万端口）。调优：内核参数 tcp_tw_reuse（复用 TIME_WAIT）、减少短连接（长连接/连接池）。

```shell
ss -tan state time-wait | wc -l        # 统计 TIME_WAIT 数量
sysctl net.ipv4.tcp_tw_reuse           # 查看复用参数
sysctl -w net.ipv4.tcp_tw_reuse=1      # 开启 TIME_WAIT 复用
```

```text
TIME_WAIT 要点：
1. 正常状态 —— 主动关闭方都要经历，约 60 秒
2. 堆积原因 —— 短连接频繁（每次请求都新建连接）
3. 端口耗尽 —— 大量 TIME_WAIT 占端口，新连接失败
4. 解法 —— tcp_tw_reuse 复用，或改用长连接/连接池
```

## Keepalive

TCP Keepalive 是「探测死连接」的机制——连接长时间没数据时，定期发探测包，对端不响应就判定连接已死并关闭。用途：防止「对端宕机但连接还挂着」导致的资源泄漏（如 SSH 长时间不操作、数据库连接池）。内核参数 tcp_keepalive_time（多久开始探测）、tcp_keepalive_intvl（探测间隔）、tcp_keepalive_probes（探测次数）。注意：TCP Keepalive 是协议层探测，和应用层心跳（HTTP keep-alive）不是一回事。

```shell
sysctl net.ipv4.tcp_keepalive_time       # 默认 7200 秒（2 小时）
sysctl net.ipv4.tcp_keepalive_intvl      # 探测间隔 75 秒
sysctl net.ipv4.tcp_keepalive_probes     # 探测次数 9 次
```

```text
TCP Keepalive vs HTTP keep-alive（别混淆）：
1. TCP Keepalive —— 传输层，探测死连接，内核参数控制
2. HTTP keep-alive —— 应用层，复用连接发多个请求，协议头控制
```

## 拥塞控制

拥塞控制是 TCP「防止把网络塞爆」的机制：通过慢启动（slow start，逐步加大发送窗口）、拥塞避免（达到阈值后线性增长）、快速重传/恢复（丢包时快速恢复）动态调整发送速率。核心是「发送窗口」——根据网络状况（丢包、RTT）自适应调整。算法有 cubic（默认）、reno、bbr（Google 的，高吞吐场景优）。Linux 用 `sysctl net.ipv4.tcp_congestion_control` 查看/设置。

```shell
sysctl net.ipv4.tcp_congestion_control   # 看当前算法（通常 cubic）
sysctl -w net.ipv4.tcp_congestion_control=bbr   # 切 bbr（高吞吐）
```

```text
拥塞控制要点：
1. 慢启动 —— 开始指数增长发送窗口
2. 拥塞避免 —— 达阈值后线性增长
3. 丢包恢复 —— 快速重传/快速恢复
4. 算法 —— cubic（默认）、bbr（Google，高吞吐/长肥管道优）
```

## 应用场景实战

### 场景一：CLOSE_WAIT 堆积，排查服务没关闭连接

```shell
# 1. 看 CLOSE_WAIT 堆积
ss -tan | awk '$1=="CLOSE-WAIT"{print $4}' | sort | uniq -c | sort -rn
# 2. 定位到具体进程
ss -tanp | grep CLOSE-WAIT
# 3. 结论：CLOSE_WAIT 说明本端服务收到 FIN 后没调 close，查代码
```

### 场景二：TIME_WAIT 过多导致端口耗尽

```shell
# 1. 统计 TIME_WAIT
ss -tan state time-wait | wc -l
# 2. 看是否接近端口上限
cat /proc/sys/net/ipv4/ip_local_port_range
# 3. 开启复用 + 调端口范围
sysctl -w net.ipv4.tcp_tw_reuse=1
```

## 最佳实践与踩坑记录

### 最佳实践

1. 用 ss -tan 看连接状态，别只看「有没有连接」，要看「状态是否异常堆积」
2. CLOSE_WAIT 堆积 = 代码没正确 close 连接（对端关了本端没关），查代码
3. TIME_WAIT 过多 = 短连接频繁，用连接池/长连接，或 tcp_tw_reuse
4. TCP Keepalive 和 HTTP keep-alive 是两回事，别混为一谈
5. 高吞吐/长肥管道场景可试 bbr 拥塞算法

### 踩坑记录

```text
坑 1：CLOSE_WAIT 堆积以为是网络问题
结论：连接堆积在 CLOSE_WAIT，以为是网络故障。
原因：CLOSE_WAIT 是本端服务收到 FIN 没 close，是代码 bug 不是网络问题。
解法：查服务代码，确保收到 FIN 后正确关闭 socket。

坑 2：TIME_WAIT 多就盲目调参数
结论：TIME_WAIT 多，直接改 tcp_tw_reuse/tw_recycle。
原因：TIME_WAIT 是正常的，盲目改参数可能引入新问题。
解法：先确认是否真的端口耗尽，优先用连接池/长连接解决。

坑 3：TCP Keepalive 和 HTTP keep-alive 搞混
结论：以为配了 HTTP keep-alive 就能探测死连接。
原因：两个是不同的东西，HTTP keep-alive 是复用连接，不探测死活。
解法：探测死连接用 TCP Keepalive（内核参数）或应用层心跳。

坑 4：SYN_SENT 堆积，连不上目标
结论：连接卡在 SYN_SENT 状态，连不上服务器。
原因：SYN 发出去了但没收到 ACK，目标不可达/防火墙挡。
解法：排查目标 IP 可达性、防火墙、目标服务是否监听。

坑 5：改拥塞算法没评估就上生产
结论：直接把算法改成 bbr，某些场景反而变差。
原因：不同算法适合不同场景，bbr 适合长肥管道，不是万能。
解法：先在测试环境压测对比，再决定是否切换。
```

## 相关文档

- [[11.1-TCP-IP 协议栈]] — TCP/IP 分层
- [[11.5-网络诊断工具]] — ss/tcpdump 看 TCP
- [[16.5-网络性能]] — TCP 调优与网络性能
- [[11.3-路由与转发]] — 网络层路由
