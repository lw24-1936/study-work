---
title: systemd
created: 2026-08-18
updated: 2026-08-18
type: concept
tags: [linux, systemd]
---

# systemd

整理日期：2026-08-18

> 状态：已完成

## 目录

- [概述](#概述)
- [unit 类型](#unit-类型)
- [systemctl 常用操作](#systemctl-常用操作)
- [依赖关系](#依赖关系)
- [启动顺序](#启动顺序)
- [应用场景实战](#应用场景实战)
- [最佳实践与踩坑记录](#最佳实践与踩坑记录)
- [相关文档](#相关文档)

## 概述

systemd 是现代 Linux 的初始化系统（PID 1）和服务管理器，负责开机拉起所有服务、管理服务生命周期（启动/停止/重启/自启）、记录日志（journald）。它用「unit」统一管理各种资源——服务（service）、定时器（timer）、目标（target）、套接字（socket）、挂载（mount）。systemctl 是管理 unit 的命令，几乎所有服务操作都靠它。

```text
systemd 核心概念：
1. unit —— 统一的管理单元，有 service/timer/target/socket/mount 等类型
2. systemctl —— 管理 unit 的命令（启动/停止/自启/查看）
3. 依赖关系 —— unit 之间的 After/Requires/Wants，决定启动顺序
4. journald —— systemd 的日志系统，journalctl 查看
```

```text
# 实测（本机 systemd 版本）
$ systemd --version | head -1
systemd 255 (255.4-1ubuntu8.17)
```

## unit 类型

unit 是 systemd 管理的资源单位，主要类型：service（服务）、timer（定时任务）、target（启动目标/运行级别）、socket（套接字激活）、mount（挂载点）、slice（资源分组）。日常接触最多的是 service（`systemctl start nginx` 就是启动 nginx.service）和 target（运行级别）。理解 unit 类型，才能看懂 systemctl 操作的对象。

```text
unit 类型速查：
1. service —— 服务（nginx/mysql/sshd），最常用
2. timer —— 定时任务（对应 cron）
3. target —— 启动目标/运行级别（multi-user/graphical）
4. socket —— 套接字激活（按需启动服务）
5. mount —— 挂载点（对应 /etc/fstab 的挂载）
6. slice —— 资源分组（cgroup 隔离）
```

```shell
systemctl list-units --type=service --state=running   # 运行中的服务
systemctl list-units --type=target --all              # 所有 target
systemctl list-unit-files --type=service              # 所有 service 单元文件
```

## systemctl 常用操作

systemctl 的日常操作：start/stop/restart（启动/停止/重启）、enable/disable（开机自启/取消自启）、status（看状态）、is-active/is-enabled（查状态）、reload（重载配置，不重启服务）、daemon-reload（重载 systemd 单元文件）。记关键区别：enable 是「开机自启」，start 是「立即启动」——两者独立，要既启动又自启就 `enable --now`。

```shell
systemctl start nginx          # 立即启动
systemctl stop nginx           # 停止
systemctl restart nginx        # 重启
systemctl reload nginx         # 重载配置（不中断服务）
systemctl enable nginx         # 开机自启
systemctl disable nginx        # 取消自启
systemctl enable --now nginx   # 自启 + 立即启动
systemctl status nginx         # 看状态
systemctl daemon-reload        # 改单元文件后重载
```

```text
enable vs start（关键区别）：
1. start —— 立即启动，但重启后不自动起
2. enable —— 设置开机自启，但不立即启动
3. enable --now —— 两者都做（自启 + 立即启动）
```

## 依赖关系

systemd 的 unit 之间有依赖关系，决定「谁先启动、谁依赖谁」。关键指令：After/Before（启动顺序）、Requires（强依赖，对方挂了我也停）、Wants（弱依赖，对方挂了我不停）、Conflicts（互斥）。这些写在 unit 文件的 [Unit] 段。理解依赖关系才能看懂「为什么服务启动有顺序」和「排查依赖导致的启动失败」。

```text
依赖关系指令（unit 文件的 [Unit] 段）：
1. After=/Before= —— 启动顺序（在某某之后/之前）
2. Requires= —— 强依赖，依赖方失败则本单元也失败
3. Wants= —— 弱依赖，依赖方失败不影响本单元
4. Conflicts= —— 互斥，不能同时启动
```

```shell
systemctl list-dependencies nginx          # 看 nginx 依赖谁
systemctl list-dependencies nginx --reverse # 看谁依赖 nginx
systemctl show nginx -p After -p Wants     # 看具体依赖指令
```

## 启动顺序

systemd 的启动是有序的：从默认 target 出发，按依赖关系（After/Requires/Wants）层层拉起服务，并行启动无依赖的服务。启动顺序的根是 default target（graphical 或 multi-user），往下是 basic.target（基础系统）→ 各服务。systemd 并行化启动（比老 SysV 串行快），但依赖关系保证「被依赖的先起」。

```text
启动顺序（从目标到服务）：
default.target（graphical/multi-user）
  → basic.target（基础系统就绪）
    → 各服务（nginx/mysql/...，按 After/Requires 排序，无依赖的并行）
```

```shell
systemctl get-default              # 默认 target
systemd-analyze critical-chain     # 看启动关键链路
systemd-analyze blame | head       # 看哪个服务拖慢启动
```

## 应用场景实战

### 场景一：查看服务状态和自启情况

```shell
# 1. 看服务是否在运行
systemctl is-active nginx
# 2. 看是否开机自启
systemctl is-enabled nginx
# 3. 看完整状态（含最近日志）
systemctl status nginx
# 4. 看服务依赖谁
systemctl list-dependencies nginx
```

### 场景二：分析开机启动慢的元凶

```shell
# 1. 总启动时间
systemd-analyze
# 2. 各服务启动耗时排行
systemd-analyze blame | head -5
```

```text
# 实测（本机）
$ systemd-analyze blame | head -3
2min 33.159s docker.service
2min 6.774s fwupd-refresh.service
46.127s containerd.service
# docker.service 拖了 2 分 33 秒，是开机慢的头号元凶
```

## 最佳实践与踩坑记录

### 最佳实践

1. 服务操作用 systemctl，别直接 kill 进程（systemd 会再拉起）
2. enable 和 start 是两回事，要自启 + 启动用 enable --now
3. 改 unit 文件后必须 systemctl daemon-reload，否则不生效
4. 服务起不来先 systemctl status + journalctl -u 看日志
5. 开机慢用 systemd-analyze blame 定位慢服务

### 踩坑记录

```text
坑 1：改 unit 文件没 daemon-reload，改动不生效
结论：改了 service 文件，systemctl restart 还是老配置。
原因：systemd 缓存了旧单元文件，要 daemon-reload 重新加载。
解法：改 unit 文件后 systemctl daemon-reload，再 restart。

坑 2：enable 了但重启后服务没起来
结论：systemctl enable nginx 后，重启 nginx 没自启。
原因：enable 只设自启，当时没 start；或依赖的服务没起来。
解法：enable --now 一次到位，重启后 systemctl status 排查依赖。

坑 3：systemctl stop 后服务又自己起来了
结论：stop 了服务，过一会又 running。
原因：服务配了 Restart=always，或 socket 激活/定时器又拉起了它。
解法：要彻底停用 disable + mask（mask 防止被激活）。

坑 4：systemctl restart 和 reload 用错
结论：改配置后 restart，服务中断了（reload 可以不断）。
原因：restart 是停止再启动（中断），reload 是重载配置（不断连接）。
原因：支持热重载的服务（nginx）用 reload，不支持才 restart。

坑 5：直接 kill 进程，systemd 反复重启它
结论：kill 了服务进程，systemd 又把它拉起来。
原因：systemd 托管服务，进程退出会按 Restart= 策略重启。
解法：正确停服务用 systemctl stop，别直接 kill。
```

## 相关文档

- [[10.3-编写 Service Unit]] — 写自定义服务单元
- [[10.4-运行级别与 target]] — target 与运行级别
- [[07.6-守护进程]] — 守护进程与 systemd 托管
- [[10.1-开机启动流程]] — systemd 在启动流程中的位置
