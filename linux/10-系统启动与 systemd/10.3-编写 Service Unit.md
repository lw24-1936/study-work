---
title: 编写 Service Unit
created: 2026-08-18
updated: 2026-08-18
type: concept
tags: [linux, systemd, service]
---

# 编写 Service Unit

整理日期：2026-08-18

> 状态：已完成

## 目录

- [概述](#概述)
- [unit 配置结构](#unit-配置结构)
- [Type 与 ExecStart 与 Restart](#type-与-execstart-与-restart)
- [日志集成](#日志集成)
- [应用场景实战](#应用场景实战)
- [最佳实践与踩坑记录](#最佳实践与踩坑记录)
- [相关文档](#相关文档)

## 概述

Service Unit 是 systemd 的服务定义文件，告诉 systemd「怎么启动、怎么重启、什么用户跑、日志去哪」。把自己写的程序托管给 systemd，就写一个 .service 文件，放到 /etc/systemd/system/ 下，就能获得开机自启、崩溃重启、日志收集。核心是三个段：[Unit]（描述+依赖）、[Service]（怎么跑，Type/ExecStart/Restart）、[Install]（怎么自启）。

```text
Service Unit 三段结构：
1. [Unit] —— Description 描述、After 启动顺序、Requires 依赖
2. [Service] —— Type 启动类型、ExecStart 启动命令、Restart 重启策略
3. [Install] —— WantedBy 指定 target，决定开机自启
```

## unit 配置结构

一个完整的 service 文件分三段：[Unit] 描述服务和依赖关系（Description、After、Requires），[Service] 定义怎么运行（Type、ExecStart、Restart、User、WorkingDirectory、Environment），[Install] 定义怎么自启（WantedBy=multi-user.target）。文件放 /etc/systemd/system/xxx.service（用户自定义）或 /usr/lib/systemd/system/（软件包自带）。

```ini
[Unit]
Description=My Application
After=network.target

[Service]
Type=simple
User=deploy
WorkingDirectory=/opt/myapp
ExecStart=/opt/myapp/bin/myapp
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
```

## Type 与 ExecStart 与 Restart

三个关键参数：Type 是「进程怎么启动/后台化」的方式（simple 默认、前台运行；forking 程序自己 fork 到后台；oneshot 执行一次就退；notify 启动后发通知）。ExecStart 是启动命令。Restart 是崩溃重启策略（no 不重启、on-failure 失败才重启、always 总是重启）。选对 Type 很关键——选错会导致「服务启动后 systemd 以为它挂了」或「以为没起来」。

```text
Type 启动类型：
1. simple —— 默认，前台运行，ExecStart 进程就是服务主体
2. forking —— 程序自己 fork 到后台（老式 daemon），需配 PIDFile
3. oneshot —— 执行一次就退出（初始化脚本）
4. notify —— 启动后发 sd_notify 通知 systemd「就绪」

Restart 重启策略：
1. no —— 不重启
2. on-failure —— 非零退出码/信号才重启
3. always —— 无论为何退出都重启（配合 RestartSec 间隔）
```

```shell
# 写完 service 文件后
systemctl daemon-reload          # 重载单元文件
systemctl enable --now myapp     # 自启 + 启动
systemctl status myapp           # 看状态
```

## 日志集成

systemd 托管服务的日志自动进 journald——服务的 stdout/stderr 会被 systemd 捕获，用 journalctl -u 服务名 查看。可以配置日志去向：默认捕获到 journald，也可以 StandardOutput=append:/path 写文件。journald 的好处是「按服务过滤、带时间戳、持久化」，比服务自己写日志文件更好管理。

```shell
journalctl -u myapp              # 看服务日志
journalctl -u myapp -f           # 实时跟踪
journalctl -u myapp --since "1 hour ago"   # 按时间过滤
```

```ini
# 日志集成配置
[Service]
ExecStart=/opt/myapp/bin/myapp
StandardOutput=journal           # 输出到 journald（默认）
StandardError=journal
# 或写文件
# StandardOutput=append:/var/log/myapp.log
```

## 应用场景实战

### 场景一：把自己写的 Java 服务托管给 systemd

```ini
# /etc/systemd/system/myapp.service
[Unit]
Description=My Java App
After=network.target

[Service]
Type=simple
User=deploy
WorkingDirectory=/opt/myapp
Environment="JAVA_OPTS=-Xmx2g -Xms2g"
ExecStart=/usr/bin/java $JAVA_OPTS -jar /opt/myapp/app.jar
Restart=on-failure
RestartSec=10

[Install]
WantedBy=multi-user.target
```

```shell
systemctl daemon-reload
systemctl enable --now myapp
systemctl status myapp
journalctl -u myapp -f
```

### 场景二：排查服务起不来的原因

```shell
# 1. 看状态和退出原因
systemctl status myapp
# 2. 看详细日志
journalctl -u myapp -n 50
# 3. 看单元文件的配置是否正确
systemctl cat myapp
# 4. 手动跑 ExecStart 命令，看报错
/opt/myapp/bin/myapp
```

## 最佳实践与踩坑记录

### 最佳实践

1. Type 选对：Java/Node 等前台程序用 simple，老式 fork 程序用 forking + PIDFile
2. 生产服务配 Restart=on-failure + RestartSec，崩溃自动拉起但避免疯狂重启
3. 用独立用户跑服务（User=deploy），别用 root
4. 服务日志交给 journald，别自己写文件（journalctl 统一查）
5. 改 service 文件后 systemctl daemon-reload，再 restart

### 踩坑记录

```text
坑 1：Type=simple 配了会 fork 的程序，服务反复重启
结论：Type=simple 配 forking 程序，systemd 以为服务秒退，反复重启。
原因：forking 程序的主进程 fork 后退了，simple 模式下 systemd 以为服务结束了。
解法：会 fork 的程序用 Type=forking + PIDFile，前台程序才用 simple。

坑 2：改 service 文件没 daemon-reload
结论：改了 ExecStart，restart 还是跑旧命令。
原因：systemd 缓存了旧单元文件。
解法：改完 systemctl daemon-reload。

坑 3：Restart=always 导致启动即崩溃的服务疯狂重启
结论：服务起不来，systemd 无限重启刷日志。
原因：always 不管为何退出都重启，崩溃服务进入死循环。
解法：用 on-failure + RestartSec 间隔，先排查崩溃原因再启动。

坑 4：环境变量/依赖路径没配，服务起不来
结论：ExecStart 命令手动能跑，systemd 跑却报找不到文件。
原因：systemd 环境精简，PATH 不完整，相对路径/环境变量缺失。
解法：用绝对路径，配 Environment=，WorkingDirectory= 指定工作目录。

坑 5：User= 指定的用户没权限，服务起不来
结论：配了 User=deploy 但服务启动失败报权限错误。
原因：deploy 用户没有该目录/文件的读写权限。
解法：确认运行用户对工作目录、日志、数据目录有权限（chown）。
```

## 相关文档

- [[10.2-systemd]] — systemctl 操作与依赖
- [[07.6-守护进程]] — 守护进程与 systemd 托管
- [[13.1-系统日志]] — journald 日志系统
- [[10.4-运行级别与 target]] — WantedBy 的 target
