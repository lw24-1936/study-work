---
title: 重定向与管道
created: 2026-08-18
updated: 2026-08-18
type: concept
tags: [linux, redirect, pipe]
---

# 重定向与管道

整理日期：2026-08-18

> 状态：已完成

## 目录

- [概述](#概述)
- [标准输入输出错误](#标准输入输出错误)
- [重定向](#重定向)
- [管道](#管道)
- [tee](#tee)
- [xargs](#xargs)
- [应用场景实战](#应用场景实战)
- [最佳实践与踩坑记录](#最佳实践与踩坑记录)
- [相关文档](#相关文档)

## 概述

重定向和管道是 Linux 命令组合的核心，也是「Unix 哲学」——每个程序做好一件事、通过管道把小的工具组合成大的能力——的落地。重定向改的是「输入从哪来、输出到哪去」，管道把一条命令的输出直接喂给下一条命令的输入。掌握了它们，一条命令行的威力顶得上几十条分开敲。

```text
三个核心机制：
1. 标准流 —— 每个进程默认三个流：stdin(0) 输入、stdout(1) 输出、stderr(2) 错误
2. 重定向 —— 把流改到文件：> 覆盖、>> 追加、2> 错误、&> 全部
3. 管道 —— 把前一条的 stdout 接到后一条的 stdin，实现数据流水线
```

## 标准输入输出错误

每个进程默认打开三个文件描述符：0 是标准输入（stdin，默认键盘）、1 是标准输出（stdout，默认屏幕）、2 是标准错误（stderr，默认也是屏幕）。把 stdout 和 stderr 分开是刻意设计——正常结果走 stdout，报错走 stderr，这样重定向时能「只要结果不要报错」或「只要报错不要结果」。理解这一点，才能看懂 `2>&1`、`&>` 这类写法的含义。

```text
三个文件描述符：
1. fd 0 = stdin 标准输入 —— 程序读数据的地方，默认键盘
2. fd 1 = stdout 标准输出 —— 程序正常结果，默认屏幕
3. fd 2 = stderr 标准错误 —— 程序报错信息，默认屏幕
关键：stdout 和 stderr 分开，重定向时可以分别处理
```

```shell
# 验证 stdout 和 stderr 是分开的
ls /etc/passwd        # 成功 → stdout
ls /nonexistent       # 失败 → stderr（报错信息）

# 分别观察：正常输出重定向走，报错还留在屏幕
ls /etc/passwd /nonexistent > out.txt   # stdout 进文件，stderr 还在屏幕
ls /etc/passwd /nonexistent 2> err.txt  # stderr 进文件，stdout 还在屏幕
```

## 重定向

重定向把标准流改到文件。核心符号：`>` 覆盖写、`>>` 追加写、`<` 从文件读入、`2>` 重定向错误流、`2>&1` 把错误流合并到输出流、`&>` 或 `>&` 把输出和错误都重定向到同一个文件。容易混淆的是 `2>&1` 和 `&>` 的区别，以及 `>` 和 `2>` 的搭配顺序（`2>&1` 必须放在 `>` 之后才生效）。

```text
重定向符号表：
1. cmd > file —— stdout 覆盖写 file
2. cmd >> file —— stdout 追加写 file
3. cmd < file —— 从 file 读入作 stdin
4. cmd 2> file —— stderr 写 file
5. cmd > file 2>&1 —— stdout 和 stderr 都进 file（顺序：先 > 再 2>&1）
6. cmd &> file —— 同上，更简写，bash 专用
```

```shell
# 覆盖与追加
echo hello > log.txt
echo world >> log.txt

# 输出和错误分开落文件
ls /ok /bad > out.txt 2> err.txt

# 合并输出和错误到同一文件
ls /ok /bad > all.txt 2>&1
ls /ok /bad &> all.txt

# 丢弃输出（黑洞）
ls /etc > /dev/null 2>&1
```

## 管道

管道 `|` 把前一条命令的 stdout 直接接到后一条命令的 stdin，省去中间临时文件。它是文本处理流水线的基础：`cat 日志 | grep 关键词 | sort | uniq -c | sort -rn | head` 这样一条链就能完成「过滤 → 去重统计 → 排序 → 取前几」的完整分析。管道传递的是 stdout，stderr 不会进管道（所以报错信息会直接打屏幕）。

```text
管道要点：
1. | 连接 stdout → stdin，数据流式传递，无需中间文件
2. 只传 stdout，stderr 不走管道（想一起传要 2>&1）
3. 典型链 —— 过滤(grep) → 处理(awk/sed) → 统计(sort/uniq) → 排序 → 取前(head)
4. 管道内命令各自是独立进程，环境变量不共享
```

```shell
# 经典分析链：统计访问日志里出现最多的 IP
cat access.log | awk '{print $1}' | sort | uniq -c | sort -rn | head

# 管道 + 重定向组合：链的结果落文件
grep ERROR app.log | sort | uniq -c > error-count.txt

# stderr 也进管道（先合并再传）
ls /ok /bad 2>&1 | grep bad
```

## tee

tee 是管道的「三通」：把 stdin 的内容一份写到文件、一份继续传给下一个管道或屏幕。它解决「既想保存又想继续处理」的矛盾——比如 `命令 | tee 结果.txt | grep 关键词`，结果完整存进文件，同时继续过滤。加 -a 是追加不覆盖。

```text
tee 用法：
1. cmd | tee file —— 输出既落文件又显示到屏幕
2. cmd | tee -a file —— 追加模式，不覆盖已有文件
3. cmd | tee file | grep x —— 落文件后继续管道处理
```

```shell
# 边看边存
./deploy.sh | tee deploy.log

# 落文件后继续过滤
cat access.log | tee all.log | grep 404

# 追加模式
echo "line" | tee -a history.log
```

## xargs

xargs 把 stdin 的内容转成命令行的参数，解决「有些命令不读 stdin、只接受参数」的问题。典型场景：`find 结果 | xargs rm`、`grep -l | xargs sed`。注意文件名含空格要用 `-0` 配合 find 的 `-print0`；`-n` 控制每次传几个参数，`-I {}` 指定参数占位位置（默认在命令末尾）。

```text
xargs 要点：
1. 作用 —— 把 stdin 转成命令行参数，喂给不接受 stdin 的命令（rm/mv/mkdir 等）
2. 空格坑 —— 文件名含空格会被拆断，配合 find -print0 + xargs -0 解决
3. 选项 —— -n 每次参数个数、-I {} 参数占位符、-p 执行前确认
```

```shell
# find 找到的文件批量删除
find /tmp -name "*.tmp" -print0 | xargs -0 rm

# 批量创建目录（参数在中间）
echo "a b c" | xargs -n1 mkdir

# 参数占位（-I {} 指定位置）
ls *.jpg | xargs -I {} mv {} images/

# 批量给找到的文件加行号统计
find . -name "*.py" | xargs wc -l
```

## 应用场景实战

### 场景一：分析 Nginx 访问日志，统计访问量前 10 的 IP 和状态码分布

```shell
# 1. 统计访问量前 10 的 IP
awk '{print $1}' /var/log/nginx/access.log | sort | uniq -c | sort -rn | head -10

# 2. 统计各状态码出现次数
awk '{print $9}' /var/log/nginx/access.log | sort | uniq -c | sort -rn

# 3. 过滤出 500 错误并单独存文件
grep ' 500 ' /var/log/nginx/access.log | tee 500-errors.log | wc -l
```

```text
# 实测（本机真实 nginx 日志）
$ awk '{print $1}' /var/log/nginx/access.log | sort | uniq -c | sort -rn | head
824 100.115.7.80
$ awk '{print $9}' /var/log/nginx/access.log | sort | uniq -c | sort -rn
527 401
279 200
18 101
# 真实发现：401（未授权）占主导，说明站点有鉴权、大量请求被拦——值得排查是否有异常探测/暴力破解。
# 101 是 WebSocket 升级握手。这一条「过滤→统计→排序」的流水线，就是企业分析访问日志的标准动作。
```

### 场景二：批量清理磁盘上的临时文件并记录清理日志

```shell
# 1. 找出 /tmp 下 7 天前的 .tmp 文件，记录清单
find /tmp -name "*.tmp" -mtime +7 -print0 | tee /tmp/cleanup-list.txt | xargs -0 -n1 echo

# 2. 确认清单后批量删除（xargs 处理空格安全的文件名）
find /tmp -name "*.tmp" -mtime +7 -print0 | xargs -0 rm -f

# 3. 统计清理了多少个
wc -l /tmp/cleanup-list.txt
```

## 最佳实践与踩坑记录

### 最佳实践

1. 分析日志用管道链一次性完成过滤/统计/排序，别存一堆中间文件
2. 既要保存又要继续处理时用 tee，比「先存再重读」高效
3. 脚本里静默不需要的输出用 > /dev/null 2>&1，需要留痕的用 >> 追加日志
4. find/xargs 处理文件名统一用 -print0 + xargs -0，防空格断词
5. 错误和正常输出分开落文件（> out 2> err），排查时能直接看到报错

### 踩坑记录

```text
坑 1：2>&1 放错位置导致错误没合并
结论：cmd 2>&1 > file 写出的文件里没有错误信息。
原因：2>&1 必须在 > file 之后，否则 stderr 先指向了旧的 stdout（屏幕）。
解法：正确顺序 cmd > file 2>&1，或直接用 cmd &> file。

坑 2：xargs 处理带空格的文件名断错
结论：find | xargs rm 遇到 "my file.txt" 被拆成两个参数。
原因：xargs 默认按空白分词，空格文件名被拆断。
解法：find -print0 | xargs -0，用 NUL 分隔符。

坑 3：管道里加 sudo 无效
结论：echo x | sudo tee file 不行，sudo 不在管道起点。
原因：管道只重定向 stdin，sudo 需要作用在写文件的命令上。
解法：用 sudo tee（echo x | sudo tee /etc/file），或 sudo sh -c '...'。

坑 4：用 > 覆盖了重要文件
结论：echo x > config.txt 把原配置覆盖了。
原因：> 是覆盖写，没有提示。
解法：重要文件用 >> 追加，或设置 noclobber（set -o noclobber 禁止覆盖）。

坑 5：管道里的命令读不到环境变量/变量不共享
结论：管道后段拿不到前段设置的变量。
原因：管道每段是独立子进程，变量作用域不共享。
解法：变量在管道外设置，或用子 shell 显式传值。
```

## 相关文档

- [[02.3-文件与目录操作]] — 文件增删改查命令
- [[03.2-grep]] — 文本过滤与搜索
- [[03.4-awk]] — 按列处理文本
- [[03.5-其他文本工具]] — sort/uniq/tr 等管道配套工具
- [[04.5-输入输出重定向]] — Shell 脚本里的高级重定向
