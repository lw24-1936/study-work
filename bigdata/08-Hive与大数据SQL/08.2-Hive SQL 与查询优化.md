---
title: Hive SQL 与查询优化
created: 2026-08-14
updated: 2026-08-14
type: concept
tags: [bigdata, hive, sql, optimization]
---

# Hive SQL 与查询优化

整理日期：2026-08-14

> 状态：已完成

## 目录

- [概述](#概述)
- [核心概念](#核心概念)
  - [Hive SQL](#hive-sql)
  - [UDF/UDAF/UDTF](#udfudafudtf)
  - [窗口函数](#窗口函数)
  - [Tez](#tez)
  - [向量化](#向量化)
  - [CBO](#cbo)
  - [谓词下推](#谓词下推)
  - [Map Join](#map-join)
  - [Skew Join](#skew-join)
  - [小文件治理](#小文件治理)
- [工作原理](#工作原理)
- [常用命令与配置](#常用命令与配置)
- [配置文件解析](#配置文件解析)
- [应用场景实战](#应用场景实战)
- [性能与调优](#性能与调优)
- [安全与权限](#安全与权限)
- [故障排查](#故障排查)
- [常见问题](#常见问题)
- [面试题](#面试题)
- [最佳实践与踩坑记录](#最佳实践与踩坑记录)
- [相关文档](#相关文档)

## 概述

Hive SQL（HiveQL）是 Hive 的查询语言，在标准 SQL 之上扩展了分区、分桶、复杂类型（ARRAY/MAP/STRUCT）、Lateral View 等大数据特性。查询优化则是把「能跑」的 SQL 变成「跑得快」的 SQL，手段分三类：逻辑优化（谓词下推、列裁剪、Join 重排）、物理执行（Tez 引擎、向量化、Map Join）和成本优化（CBO 基于统计信息选计划）。

```text
Hive 查询优化全景：
1. 语法层 —— HiveQL 扩展：分区、复杂类型、Lateral View、SORT BY/CLUSTER BY
2. 逻辑优化 —— 谓词下推、列裁剪、常量折叠、Join 重排，减少读写量
3. 执行引擎 —— Tez 有向无环图 + 向量化 + LLAP，替代慢速 MapReduce
4. 物理优化 —— Map Join 小表广播、Skew Join 倾斜拆分、分桶 Map Join
5. 成本优化 —— CBO 依赖统计信息选择最优 Join 顺序与计划
```

## 核心概念

这一篇围绕 Hive SQL 的「语法方言」和「查询优化」两条主线展开：前半部分讲 HiveQL 语言本身（复杂类型、函数扩展、窗口函数），后半部分讲执行引擎与优化手段（Tez、向量化、CBO、谓词下推、Map Join、Skew Join、小文件治理）。每个概念都配有可运行的 SQL/Java 示例，理解「是什么、怎么写、怎么优化」即可掌握 Hive SQL 的核心。

### Hive SQL

HiveQL 基本兼容标准 SQL 的 SELECT/JOIN/GROUP BY/窗口函数，并有自己的扩展：分区列读写、复杂类型（ARRAY/MAP/STRUCT）、Lateral View + explode 打平数组、SORT BY（单 reducer 内排序）/DISTRIBUTE BY（按 key 分 reducer）/CLUSTER BY（前两者合并）、以及 INSERT OVERWRITE/INTO 写入语义。理解这些方言差异是写好 Hive SQL 的前提。

```text
HiveQL 与标准 SQL 的差异：
1. 复杂类型 —— ARRAY/MAP/STRUCT + Lateral View explode 打平嵌套数据
2. DISTRIBUTE BY/SORT BY —— 控制数据分发到哪个 reducer 与 reducer 内排序
3. 写入语义 —— INSERT OVERWRITE（覆盖分区）与 INSERT INTO（追加）
4. 不支持行级更新 —— 表是 append-only，更新靠重建分区（或用 ACID 事务表）
```

```sql
-- 复杂类型：解析 JSON/嵌套字段
CREATE TABLE events (
    event_id   BIGINT,
    payload    MAP<STRING,STRING>,
    tags       ARRAY<STRING>
) STORED AS PARQUET;

-- Lateral View + explode 打平数组
SELECT event_id, tag
FROM events LATERAL VIEW explode(tags) t AS tag;

-- 控制分发与排序：按 user_id 分发，reducer 内按金额降序
SELECT user_id, amount
FROM ods_orders
DISTRIBUTE BY user_id SORT BY amount DESC;

-- 覆盖写分区
INSERT OVERWRITE TABLE dwd_orders PARTITION (dt='2024-08-14')
SELECT * FROM ods_orders WHERE dt='2024-08-14';
```

### UDF/UDAF/UDTF

UDF 是 Hive 的函数扩展机制：UDF（一进一出，如大小写转换）、UDAF（多进一出，聚合）、UDTF（一进多出，如 explode 打平）。内置函数不够用时（业务专属加解密、复杂解析、自定义聚合），用 Java/Scala 写自定义函数，打成 jar 后 ADD JAR 注册。这是 Hive 可扩展性的核心。

```text
三类函数：
1. UDF —— 一行进一行出，如 parse_user_agent、脱敏、解码
2. UDAF —— 多行进一行出，自定义聚合（如中位数、分位数）
3. UDTF —— 一行进多行出，如 explode、自定义字符串切分
4. 注册方式 —— 临时函数（CREATE TEMPORARY FUNCTION，会话级）/ 永久函数（库级）
```

```java
// 自定义 UDF：字符串脱敏（保留首尾，中间打星）
import org.apache.hadoop.hive.ql.exec.UDF;

public class MaskUDF extends UDF {
    public String evaluate(String s) {
        if (s == null || s.length() <= 2) return s;
        StringBuilder sb = new StringBuilder();
        sb.append(s.charAt(0));
        for (int i = 1; i < s.length() - 1; i++) sb.append('*');
        sb.append(s.charAt(s.length() - 1));
        return sb.toString();
    }
}
```

```sql
-- 打包成 hive-mask.jar 后注册使用
ADD JAR /opt/lib/hive-mask.jar;
CREATE TEMPORARY FUNCTION mask AS 'com.example.MaskUDF';

SELECT user_id, mask(user_name) AS masked_name FROM users;
```

### 窗口函数

窗口函数在「不折叠行」的前提下做跨行计算，是 Hive 分析最常用的能力：ROW_NUMBER/RANK/DENSE_RANK 排名、SUM/AVG 运行累计、LAG/LEAD 取前后行、FIRST_VALUE/LAST_VALUE 取窗口边界。语法 OVER (PARTITION BY ... ORDER BY ... [ROWS BETWEEN ...])，其中 ROWS BETWEEN 定义物理窗口帧（如 ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW）。

```text
窗口函数要点：
1. 排名三兄弟 —— ROW_NUMBER 唯一连续、RANK 并列跳号、DENSE_RANK 并列不跳号
2. 运行累计 —— SUM(...) OVER (ORDER BY ...) 默认帧是「首行到当前行」
3. LAG/LEAD —— 取前/后第 N 行，环比、留存、相邻时间差常用
4. 窗口帧 —— ROWS BETWEEN n PRECEDING AND n FOLLOWING 精确控制范围
```

```sql
-- 每个用户按金额排名，取每个用户 Top 3 订单
SELECT user_id, order_id, amount FROM (
    SELECT user_id, order_id, amount,
           ROW_NUMBER() OVER (PARTITION BY user_id ORDER BY amount DESC) AS rn
    FROM ods_orders
) t WHERE rn <= 3;

-- 运行累计：用户累计消费
SELECT user_id, order_id, amount,
       SUM(amount) OVER (PARTITION BY user_id ORDER BY created_at
                         ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS cum_amount
FROM ods_orders;

-- LAG 算相邻订单间隔
SELECT order_id, created_at,
       LAG(created_at, 1) OVER (PARTITION BY user_id ORDER BY created_at) AS prev_time
FROM ods_orders;
```

### Tez

Tez 是 Hive 的默认执行引擎（Hive 2.x 起），用有向无环图（DAG）描述作业，把 MapReduce 的多阶段（多个 job）合并成一个 DAG，中间结果尽量在内存/本地传递而不落盘，显著减少启动开销与磁盘 IO。相比 MapReduce，Tez 通常能带来 2~5 倍的性能提升，还能配合 LLAP 做常驻内存的交互式查询。

```text
Tez 要点：
1. DAG 执行 —— 一个 Tez 作业对应一个 DAG，顶点间数据边传输，避免多 job 串行
2. 少落盘 —— 中间数据用内存 + 本地磁盘缓存，减少 shuffle 写读
3. Container 复用 —— 任务在容器里复用，省去反复申请释放的开销
4. 配合 LLAP —— 常驻内存执行，秒级交互查询
```

```xml
<!-- 开启 Tez 执行引擎 -->
<configuration>
  <property>
    <name>hive.execution.engine</name>
    <value>tez</value>
  </property>
  <property>
    <name>hive.tez.container.size</name>
    <value>4096</value>
  </property>
</configuration>
```

```shell
# 会话级切换引擎
SET hive.execution.engine = tez;

# 查看查询实际用的执行引擎与进度
SET hive.tez.exec.inplace.progress = true;
```

### 向量化

向量化执行（Vectorized Execution）把「一行一行处理」改成「一批一批处理」（默认 1024 行一批），利用 CPU 的 SIMD 指令和列式存储，大幅减少函数调用与分支开销。向量化只对 ORC/Parquet 等列式格式生效，是 Hive 提速的「免费午餐」——开启即可享受，无需改 SQL。

```text
向量化要点：
1. 批量处理 —— 一次处理一批（1024 行）而非一行，减少虚函数调用
2. 依赖列式存储 —— 只对 ORC/Parquet 生效，TextFile 不支持
3. SIMD 加速 —— 利用 CPU 单指令多数据并行算多行
4. 开启方式 —— hive.vectorized.execution.enabled=true（Hive 2.x 默认已开）
```

```sql
-- 会话级开启向量化
SET hive.vectorized.execution.enabled = true;
SET hive.vectorized.execution.reduce.enabled = true;

-- 只在 ORC 表上生效；查看计划确认 vectorized
EXPLAIN SELECT COUNT(*), SUM(amount) FROM dwd_orders WHERE dt='2024-08-14';
```

### CBO

CBO（Cost-Based Optimizer，基于成本的优化器）用表的统计信息（行数、列基数、数据分布）估算各执行计划的代价，选出代价最小的计划，核心收益是自动决定最优的 Join 顺序与 Join 策略。要让 CBO 生效，必须先用 ANALYZE TABLE 收集统计信息，否则退化为基于规则的 RBO。

```text
CBO 要点：
1. 成本模型 —— 根据行数、基数、IO/CPU 代价估算每个计划的成本
2. Join 顺序 —— 自动把小结果集放前面，减少中间数据量
3. 依赖统计信息 —— 需 ANALYZE TABLE ... COMPUTE STATISTICS 采集
4. 用 Calcite —— Hive CBO 基于 Apache Calcite 实现
```

```sql
-- 收集表统计信息（行数）
ANALYZE TABLE dwd_orders COMPUTE STATISTICS;

-- 收集列统计信息（基数、分布）
ANALYZE TABLE dwd_orders COMPUTE STATISTICS FOR COLUMNS user_id, status;

-- 开启 CBO（默认已开）
SET hive.cbo.enable = true;

-- 查看 CBO 选择的执行计划
EXPLAIN SELECT o.*, p.user_name
FROM dwd_orders o JOIN dim_user p ON o.user_id = p.user_id;
```

### 谓词下推

谓词下推（Predicate Pushdown）把 WHERE 过滤条件尽量提前执行、下推到存储层，在读取数据时就过滤掉不需要的行，减少 IO 与后续处理量。配合列式存储（ORC/Parquet），还能同时做「列裁剪 + 谓词下推」，只读相关列相关行。Hive 的 ORC/Parquet 存储层原生支持把条件推给文件格式层过滤。

```text
谓词下推要点：
1. 尽早过滤 —— 条件在 Map 端 / 存储层就执行，不把脏数据传到 Reduce
2. 存储层下推 —— ORC/Parquet 用 stripe/row group 的 min-max 统计跳过整块数据
3. Join 下推 —— ON 条件能下推到子查询，减少参与 Join 的行
4. 失效场景 —— 函数包裹列、UDF、OR 条件可能阻断下推
```

```sql
-- 可下推：dt 与 status 都能在存储层过滤
SELECT order_id, amount FROM dwd_orders
WHERE dt = '2024-08-14' AND status = 'paid';

-- Join 谓词下推：先在子查询过滤再 Join，减少 shuffle 数据量
SELECT o.order_id, p.user_name
FROM (SELECT * FROM dwd_orders WHERE dt = '2024-08-14') o
JOIN dim_user p ON o.user_id = p.user_id;
```

### Map Join

Map Join（也叫 Broadcast Join）把一张小表完整加载到每个 Map 端的内存哈希表，大表在 Map 端直接查表关联，完全避免 shuffle 和 reduce 阶段。这是 Hive 最重要的 Join 优化——当一张表足够小（默认 25MB 以内）时，Hive 自动把小表广播，把昂贵的 shuffle Join 变成廉价的本地 Join。

```text
Map Join 要点：
1. 原理 —— 小表广播到所有 Mapper 内存，大表流式关联，无 shuffle
2. 阈值 —— hive.auto.convert.join.noconditionaltask.size 默认约 25MB
3. 手动指定 —— /*+ MAPJOIN(small_table) */ hint 强制小表广播
4. 适用 —— 事实表 JOIN 维表（维表通常很小），数仓最经典场景
```

```sql
-- 自动 Map Join：维表 < 25MB 时自动广播
SET hive.auto.convert.join = true;
SET hive.auto.convert.join.noconditionaltask.size = 25600000;

SELECT o.order_id, p.user_name, o.amount
FROM dwd_orders o JOIN dim_user p ON o.user_id = p.user_id;

-- 手动 hint 强制 Map Join
SELECT /*+ MAPJOIN(p) */ o.order_id, p.user_name
FROM dwd_orders o JOIN dim_user p ON o.user_id = p.user_id;
```

### Skew Join

Skew Join 解决数据倾斜：当 Join key 分布严重不均（如某几个热门 key 占了大部分数据），部分 reduce 任务被压垮。Hive 把倾斜的 key 单独抽出来做 Map Join，其余 key 正常 shuffle Join，从而均衡负载。开启后 Hive 自动检测超过阈值的倾斜 key 并拆分处理。

```text
Skew Join 要点：
1. 倾斜识别 —— 统计 key 分布，超过 hive.skewjoin.key 阈值（如 10 万行）判为倾斜
2. 拆分处理 —— 倾斜 key 单独 Map Join，非倾斜 key 走普通 Join
3. 场景 —— 热门商品、大 V、默认值（NULL/空串/0）集中导致的倾斜
4. 治本 —— 提前把 NULL/空值打散，或给 key 加随机前缀
```

```sql
-- 开启 Skew Join
SET hive.optimize.skewjoin = true;
SET hive.skewjoin.key = 100000;

SELECT o.order_id, p.user_name
FROM dwd_orders o JOIN dim_user p ON o.user_id = p.user_id;

-- 治本：NULL 键加随机后缀打散
SELECT o.order_id, p.user_name
FROM dwd_orders o JOIN dim_user p
ON (CASE WHEN o.user_id IS NULL
         THEN CONCAT('null_', CAST(RAND() * 100 AS INT))
         ELSE o.user_id END) = p.user_id;
```

### 小文件治理

小文件是 Hive 最大的性能杀手之一：大量小文件（远小于块大小）导致 NameNode 元数据膨胀、Map 任务数爆炸、每个 Mapper 启动开销大于处理开销。治理手段分事前（合并写入、控制 reducer 数、用 Concatenate）与事后（定期合并小文件），以及用 ORC/Parquet 的合并工具与 Hive 的 concatenate 命令。

```text
小文件治理要点：
1. 危害 —— 元数据膨胀 + Mapper 数爆炸 + 启动开销 > 计算开销
2. 事前 —— 控制 reducer 数、关闭动态分区过细、用桶表
3. Concatenate —— ALTER TABLE ... CONCATENATE 合并 ORC 同分区小文件
4. 事后 —— 定期用 Spark/Hive 重写分区，或 HDFS 层面合并
```

```sql
-- 合并 ORC 表某分区的小文件
ALTER TABLE dwd_orders PARTITION (dt='2024-08-14') CONCATENATE;

-- 控制写入产生的文件数
SET hive.merge.mapfiles = true;
SET hive.merge.mapredfiles = true;
SET hive.merge.smallfiles.avgsize = 16000000;
SET hive.merge.size.per.task = 256000000;

-- 重写分区合并小文件
INSERT OVERWRITE TABLE dwd_orders PARTITION (dt='2024-08-14')
SELECT * FROM dwd_orders WHERE dt='2024-08-14';
```

## 工作原理

Hive SQL 要变成真正跑在集群上的分布式计算任务，要经过「解析 -> 语义分析 -> 逻辑优化 -> 物理优化（CBO）-> 任务生成 -> 执行」六步。理解这条链路，才能看懂 EXPLAIN 的执行计划，也才能判断某个优化（谓词下推、Map Join、CBO 选 Join 顺序）到底在哪一步生效。

```text
Hive SQL 从文本到结果的完整链路：
1. 解析(Parser) —— 把 SQL 文本解析成 AST 抽象语法树，语法错误在这一步报出
2. 语义分析(Semantic Analyzer) —— 绑定 Metastore 元数据，校验表/列/类型，生成逻辑计划(Operator Tree)
3. 逻辑优化(Logical Optimizer) —— 与引擎无关的规则改写：谓词下推、列裁剪、常量折叠、分区裁剪、Join 重排
4. 物理优化(CBO) —— 基于统计信息估算代价，决定 Join 顺序与 Join 策略(Map Join/Skew Join)
5. 任务生成 —— 把物理计划翻译成 Tez DAG / Spark RDD / MapReduce 任务
6. 执行 —— 提交 YARN，向量化批量计算，中间结果尽量内存/本地传递
```

```text
两层优化器各管什么：
1. RBO(Rule-Based) —— 靠固定规则改写，如「谓词下推」「列裁剪」「常量折叠」，不依赖数据统计
2. CBO(Cost-Based) —— 靠统计信息估算每个计划的代价，选最小代价计划，核心收益是 Join 顺序与 Join 策略
3. 关系 —— RBO 先做规则改写，CBO 再做代价选择，两者叠加才有最优计划
4. 前提 —— CBO 要统计信息（ANALYZE TABLE），否则退化成 RBO
```

## 常用命令与配置

```shell
# 命令行执行 SQL
hive -e "SELECT count(*) FROM dwd_orders WHERE dt='2024-08-14'"
hive -f etl.sql                          # 执行 SQL 文件
hive --database ods -f etl.sql           # 指定库执行

# JDBC 连接 HiveServer2
beeline -u "jdbc:hive2://hiveserver2:10000/default" -n hive -p hive

# 收集统计信息（CBO 前提）
hive -e "ANALYZE TABLE dwd_orders COMPUTE STATISTICS FOR COLUMNS user_id, status"
```

```sql
-- 查看执行计划（判断优化是否生效看这里）
EXPLAIN SELECT o.order_id, p.user_name
FROM dwd_orders o JOIN dim_user p ON o.user_id = p.user_id;

-- 更详细的计划（含 Map Join/谓词下推信息）
EXPLAIN EXTENDED SELECT ...;

-- 查看 CBO 优化后的计划
EXPLAIN CBO SELECT ...;
```

```text
常用开关速查（会话级 SET）：
1. hive.execution.engine=tez —— 切 Tez 引擎
2. hive.auto.convert.join=true —— 自动 Map Join
3. hive.optimize.skewjoin=true —— Skew Join 倾斜拆分
4. hive.vectorized.execution.enabled=true —— 向量化
5. hive.cbo.enable=true —— CBO 优化器
6. hive.merge.mapfiles=true —— 小文件合并
```

## 配置文件解析

```xml
<!-- hive-site.xml：查询优化关键配置 -->
<configuration>
  <property>
    <name>hive.execution.engine</name>                          <!-- 执行引擎 mr/tez/spark -->
    <value>tez</value>
  </property>
  <property>
    <name>hive.auto.convert.join</name>                          <!-- 自动 Map Join 开关 -->
    <value>true</value>
  </property>
  <property>
    <name>hive.auto.convert.join.noconditionaltask.size</name>   <!-- 广播阈值(字节)，默认约 25MB -->
    <value>25600000</value>
  </property>
  <property>
    <name>hive.vectorized.execution.enabled</name>               <!-- 向量化开关 -->
    <value>true</value>
  </property>
  <property>
    <name>hive.cbo.enable</name>                                 <!-- CBO 优化器开关 -->
    <value>true</value>
  </property>
  <property>
    <name>hive.optimize.skewjoin</name>                          <!-- Skew Join 开关 -->
    <value>true</value>
  </property>
  <property>
    <name>hive.merge.mapfiles</name>                             <!-- Map 端小文件合并 -->
    <value>true</value>
  </property>
</configuration>
```

```text
关键字段含义：
hive.execution.engine                         计算引擎，tez 比 mr 快 2~5 倍
hive.auto.convert.join.noconditionaltask.size 决定「多大算小表」的广播阈值
hive.vectorized.execution.enabled             向量化，只对 ORC/Parquet 生效
hive.cbo.enable                               CBO 开关，依赖 ANALYZE 统计信息
hive.optimize.skewjoin                        数据倾斜时自动拆分倾斜 key
hive.merge.mapfiles / hive.merge.mapredfiles  Map/Reduce 端小文件合并
```

## 应用场景实战

### 场景一：大表 Join 维表 + 倾斜 key 优化

```sql
-- 事实表（百亿级）关联维表，经典 Map Join + 倾斜处理
SET hive.auto.convert.join = true;
SET hive.optimize.skewjoin = true;

-- 维表不算小（>25MB），手动裁剪列缩小后广播
WITH small_dim AS (
    SELECT user_id, user_name, city
    FROM dim_user WHERE city IN ('北京','上海','深圳')
)
SELECT /*+ MAPJOIN(small_dim) */ o.order_id, small_dim.user_name, o.amount
FROM dwd_orders o JOIN small_dim ON o.user_id = small_dim.user_id
WHERE o.dt = '2024-08-14';
```

### 场景二：用窗口函数构建用户 RFM 画像

```sql
-- RFM：最近消费时间（R）、消费频次（F）、消费金额（M）
WITH rfm AS (
    SELECT user_id,
           DATEDIFF('2024-08-14', MAX(created_at)) AS recency,
           COUNT(DISTINCT order_id) AS frequency,
           SUM(amount) AS monetary
    FROM dwd_orders
    WHERE dt BETWEEN '2024-05-01' AND '2024-08-14'
    GROUP BY user_id
)
SELECT user_id,
       CASE WHEN recency <= 30 THEN '高' ELSE '低' END AS r_level,
       NTILE(4) OVER (ORDER BY frequency DESC) AS f_quartile,
       NTILE(4) OVER (ORDER BY monetary DESC) AS m_quartile
FROM rfm;
```

### 场景三：用户留存分析（LAG 自关联 + 次日留存）

```sql
-- 次日留存：今天访问的用户，明天是否还来
-- 1. 用户每日访问去重
WITH daily_active AS (
    SELECT user_id, dt
    FROM dwd_visit
    WHERE dt BETWEEN '2024-08-01' AND '2024-08-14'
    GROUP BY user_id, dt
),
-- 2. 用 LAG 算用户下次访问日期
gap AS (
    SELECT user_id, dt,
           LEAD(dt, 1) OVER (PARTITION BY user_id ORDER BY dt) AS next_dt
    FROM daily_active
)
-- 3. 次日留存率 = 相邻访问间隔为 1 天的用户占比
SELECT dt,
       COUNT(DISTINCT user_id) AS active_users,
       SUM(IF(DATEDIFF(next_dt, dt) = 1, 1, 0)) AS next_day_retained
FROM gap
GROUP BY dt
ORDER BY dt;
```

### 场景四：动态分区写入 + 小文件合并

```sql
-- 按小时动态分区写入，同时控制文件数，避免小文件爆炸
SET hive.exec.dynamic.partition = true;
SET hive.exec.dynamic.partition.mode = nonstrict;
SET hive.merge.mapfiles = true;
SET hive.merge.mapredfiles = true;

INSERT OVERWRITE TABLE dws_order_hour PARTITION (dt, hr)
SELECT user_id, SUM(amount) AS gmv, dt, hour(created_at) AS hr
FROM dwd_orders
WHERE dt = '2024-08-14'
GROUP BY user_id, dt, hour(created_at);

-- 事后合并某个小时分区的小文件（ORC）
ALTER TABLE dws_order_hour PARTITION (dt='2024-08-14', hr='10') CONCATENATE;
```

## 性能与调优

```text
Hive SQL 调优分层清单（从 IO 到计算再到执行）：

一、存储与读层（省 IO）
1. 分区裁剪 —— where 命中分区列，扫描量下降几个数量级
2. 列式存储 + 列裁剪 —— ORC/Parquet 只读用到的列
3. 谓词下推 —— 过滤条件下推到存储层，用 min-max 统计跳过整块
4. 避免函数包裹分区列 —— WHERE year(dt)=2024 会全表扫描

二、Join 层（省 shuffle）
5. 维表走 Map Join —— 小表广播到 Map 端内存，免 shuffle
6. 先过滤后 Join —— 子查询里先 WHERE，减少参与 Join 的行
7. 数据倾斜处理 —— Skew Join / NULL 打散 / 两阶段聚合
8. 大表 Join 顺序 —— 让 CBO 统计信息决定，别硬写顺序

三、引擎与执行层（省 CPU/启动开销）
9. Tez 引擎替代 MapReduce —— DAG 合并多阶段，少落盘
10. 向量化 —— 批量处理，免费提速
11. 合理 reducer 数 —— 太多启动开销大，太少单任务拖尾
12. 小文件合并 —— 控制 Mapper 数，避免元数据膨胀
```

```sql
-- 一次性开齐常用优化开关（调优基线）
SET hive.execution.engine = tez;
SET hive.vectorized.execution.enabled = true;
SET hive.auto.convert.join = true;
SET hive.cbo.enable = true;
SET hive.optimize.skewjoin = true;
SET hive.merge.mapfiles = true;

-- 定期刷新统计信息，让 CBO 有据可依
ANALYZE TABLE dwd_orders COMPUTE STATISTICS;
ANALYZE TABLE dwd_orders COMPUTE STATISTICS FOR COLUMNS user_id, status;
```

## 安全与权限

```text
Hive 安全三层体系（认证 -> 授权 -> 审计）：

一、认证（确认「你是谁」）
1. HiveServer2 支持 Kerberos / LDAP / PAM / CUSTOM 多种认证
2. Kerberos —— 集群标准认证，beeline 需带 principal 连接
3. LDAP —— 企业统一账号体系，HiveServer2 直接对接

二、授权（确认「你能访问什么」）
1. SQL 标准授权(SQL Standards Based Authorization) —— 基于 Hive 自身的 GRANT/REVOKE
2. Ranger —— 企业主流方案，集中式策略管理，支持库/表/列级权限
3. 细粒度 —— Ranger 支持行级过滤(row-level filtering)与列级脱敏(masking)

三、审计（确认「谁做了什么」）
1. Ranger Audit —— 记录所有访问行为，满足合规
2. HiveServer2 审计日志 —— 记录连接、查询、DDL 操作

四、脱敏要点
1. 敏感列(身份证/手机号/卡号)用 Ranger masking 策略脱敏
2. 脱敏分「掩码/替换/哈希」等类型，按角色差异化下发
3. 行级过滤(row filter)实现「同一张表不同人看不同行」
```

```text
生产安全实践：
1. 禁用 hive.metastore.uris 裸奔，Metastore 也要 Kerberos 认证
2. 权限走 Ranger 统一管理，别各表各配 GRANT 造成混乱
3. 敏感表建视图 + 列脱敏，下游只读脱敏视图
4. 审计日志集中归档，权限变更留痕
```

## 故障排查

```text
排查思路（从现象到根因）：慢在哪 -> 看 YARN 任务 -> 看执行计划 -> 看统计信息/配置

常见故障与定位：
1. 查询突然变慢 —— 对比执行计划(EXPLAIN)，看是否 Map Join 退化、是否全表扫描
2. 长尾任务/个别 reduce 卡死 —— 数据倾斜，看 key 分布，开 Skew Join
3. CBO 选错 Join 顺序 —— 统计信息过期，重新 ANALYZE TABLE
4. Map Join 内存溢出(OOM) —— 广播表其实很大，调小阈值或改普通 Join
5. 小文件导致 Mapper 上万 —— 查分区文件数，做小文件合并
6. 向量化不生效 —— 表是 TextFile 或用了不支持的 UDF，EXPLAIN 确认
```

```shell
# 查看 Hive 任务在 YARN 上的状态与日志
yarn application -list                        # 列出所有运行中的应用
yarn application -status <app_id>             # 查看应用状态
yarn logs -applicationId <app_id>             # 拉取任务日志（看报错/倾斜）

# 查看某个分区的文件数与大小（判断小文件）
hdfs dfs -ls /warehouse/ods/ods_orders/dt=2024-08-14 | wc -l
hdfs dfs -du -h /warehouse/ods/ods_orders/dt=2024-08-14

# 查看表的统计信息是否过期
hive -e "DESCRIBE FORMATTED dwd_orders"
```

## 常见问题

```text
Q1: Map Join 为什么不生效，还是走了 shuffle Join？
A: 小表超了广播阈值(hive.auto.convert.join.noconditionaltask.size 默认约 25MB)，Hive 自动回退成普通 Join。确认表真实大小，超阈值就裁剪列、调大阈值，或加 /*+ MAPJOIN(小表) */ hint 强制。

Q2: 数据倾斜怎么定位和解决？
A: 定位看 YARN 任务是否有长尾(个别 reduce 跑很久)，再看 key 分布(COUNT GROUP BY key)。解决：开 Skew Join；NULL/空值 key 加随机前缀打散；两阶段聚合；或把倾斜 key 单独抽出来 Map Join。

Q3: 开了 CBO 但 Join 顺序还是不对？
A: CBO 依赖统计信息，没跑 ANALYZE TABLE 或统计信息过期时，CBO 只能靠估算，可能选错计划。定期 ANALYZE TABLE ... COMPUTE STATISTICS FOR COLUMNS 刷新行数与基数。

Q4: WHERE 条件没被下推，全表扫描了？
A: 常见原因是函数包裹了分区/列(如 year(dt)、substr(id))，或用了 UDF，优化器无法把条件推到存储层。改成裸列范围比较 WHERE dt BETWEEN '2024-01-01' AND '2024-12-31'。

Q5: 向量化为什么没提速，甚至报错？
A: 向量化只对 ORC/Parquet 列式格式生效，TextFile 不支持；某些 UDF 或复杂类型也不支持，会自动退化为逐行执行。确认表格式，EXPLAIN 看计划里是否有 vectorized 标记。

Q6: 小文件怎么判断，怎么治理？
A: 判断看分区文件数是否远超 Mapper 期望值、单文件是否远小于块大小(128MB/256MB)。治理：写时用 hive.merge.* 参数合并 + 控制 reducer 数；写后 ALTER TABLE ... CONCATENATE 或 INSERT OVERWRITE 重写分区。

Q7: 窗口函数和 GROUP BY 有什么区别，什么时候用哪个？
A: GROUP BY 会折叠行(每组合并成一行)，适合聚合汇总；窗口函数不折叠行，在保留明细的同时算排名/累计/前后行。要「保留每行明细 + 跨行计算」就用窗口函数，要「分组汇总」用 GROUP BY。

Q8: INSERT OVERWRITE 和 INSERT INTO 有什么区别？
A: OVERWRITE 覆盖目标分区(分区内先清空再写)，INTO 追加。数仓重跑某天任务用 OVERWRITE 保证幂等，避免重复数据。注意 OVERWRITE 只覆盖命中分区，不影响其它分区。
```

## 面试题

```text
1. Map Join 和 Reduce Join 的原理区别？Map Join 有什么限制？
   答：Reduce Join 把两表都按 key shuffle 到同一 reduce 做关联，有 shuffle 和 reduce 阶段；Map Join 把小表广播到每个 Mapper 内存建哈希表，大表在 Map 端直接关联，无 shuffle。限制：小表必须能装进 Mapper 内存(默认 25MB 阈值)，否则 OOM。

2. 数据倾斜是怎么产生的？有哪些解决方案？
   答：Join 或聚合时 key 分布严重不均，个别 key 数据量巨大，导致少量 reduce 长尾。方案：Skew Join 拆分倾斜 key；NULL/空值加随机前缀打散；两阶段聚合(先局部聚合再全局聚合)；调整 reduce 数。

3. 谓词下推的原理是什么？什么情况会失效？
   答：把 WHERE 过滤条件提前到存储层/Map 端执行，配合列式存储的 min-max 统计跳过整块数据，减少 IO。失效：函数包裹列(破坏列与统计的映射)、UDF、复杂 OR 条件等，优化器无法安全下推。

4. CBO 和 RBO 的区别？CBO 生效的前提是什么？
   答：RBO 基于固定规则改写(谓词下推、列裁剪、常量折叠)，不依赖数据；CBO 基于统计信息估算代价选最小代价计划，核心收益是自动决定 Join 顺序与 Join 策略。CBO 前提是 ANALYZE TABLE 收集了统计信息。

5. ROW_NUMBER、RANK、DENSE_RANK 的区别？
   答：ROW_NUMBER 相同值也编唯一连续序号(1,2,3)；RANK 相同值并列同号且跳号(1,1,3)；DENSE_RANK 相同值并列但不跳号(1,1,2)。取 Top N 去重用 ROW_NUMBER，排名用 RANK/DENSE_RANK。

6. UDF、UDAF、UDTF 的区别？各举一个例子？
   答：UDF 一行进一行出(大小写转换、脱敏)；UDAF 多行进一行出(自定义聚合，如中位数)；UDTF 一行进多行出(explode 打平数组)。UDF 最常用，UDTF 常配 Lateral View。

7. 小文件的危害是什么？怎么治理？
   答：危害是 NameNode 元数据膨胀、Mapper 数爆炸、启动开销大于计算开销。治理：写时控制 reducer 数 + hive.merge.* 合并；写后 CONCATENATE 或 INSERT OVERWRITE 重写分区；用桶表减少文件数。

8. Hive 为什么用 Tez/Spark 替代 MapReduce？
   答：MapReduce 每个阶段落盘、多 job 串行启动开销大；Tez 用 DAG 合并多阶段、中间结果内存/本地传递、Container 复用，通常快 2~5 倍。配合向量化 + LLAP 可做到秒级交互查询。
```

## 最佳实践与踩坑记录

### 最佳实践

1. 优先用 Tez 引擎 + 向量化 + ORC，三件套是 Hive 提速的基本盘
2. Join 前先过滤，谓词下推 + 列裁剪减少读写量
3. 维表 Join 事实表走 Map Join，维表超阈值时手动 hint 或裁剪列
4. 定期 ANALYZE TABLE 收集统计信息，让 CBO 有据可依
5. 窗口函数替代自关联，避免重复扫表
6. 动态分区写入控制 reducer 数，配合 merge 参数减少小文件
7. 大表按 dt 分区 + 分桶，配合分区裁剪与桶 Map Join
8. 重跑任务用 INSERT OVERWRITE 保证幂等，避免重复数据
9. 敏感列建脱敏视图，下游只读脱敏视图，权限统一走 Ranger

### 踩坑记录

```text
坑 1：小表其实不小导致 Map Join 失效
结论：以为维表小，实际超阈值，Hive 回退成 shuffle Join，跑得很慢。
原因：hive.auto.convert.join.noconditionaltask.size 默认约 25MB，超了就退化成普通 Join。
解法：确认维表实际大小，超阈值时裁剪列或调大阈值 + 给广播端加内存。

坑 2：数据倾斜导致长尾任务
结论：个别 reduce 跑几小时，其他任务几秒完成，作业被拖垮。
原因：热门 key 集中到同一 reduce，负载严重不均。
解法：开启 Skew Join；或对 NULL/空值 key 加随机前缀打散；或两阶段聚合。

坑 3：忘记收集统计信息导致 CBO 选错计划
结论：开了 CBO 但没统计信息，Join 顺序反而更差。
原因：CBO 依赖 ANALYZE TABLE 的行数与基数，没有就退化为估算。
解法：建表/导入后定期 ANALYZE TABLE ... COMPUTE STATISTICS。

坑 4：函数包裹列导致谓词下推失效
结论：WHERE year(created_at)=2024 全表扫描，比 dt 分区裁剪慢几个量级。
原因：函数破坏了列与分区/统计的映射，优化器无法下推。
解法：用裸列范围比较 WHERE dt BETWEEN '2024-01-01' AND '2024-12-31'。

坑 5：小文件爆炸拖慢整个集群
结论：ETL 后产生几十万小文件，NameNode 告警，查询 Mapper 数上万。
原因：动态分区过细 + reducer 过多，每个分区每 reducer 一个文件。
解法：合并小文件（CONCATENATE/重写），控制分区粒度与 reducer 数。

坑 6：广播表过大导致 Mapper OOM
结论：强加 MAPJOIN hint 后 Map 任务频繁内存溢出、作业失败。
原因：小表压缩后或含大字段时实际内存占用远超阈值，Mapper 堆不够。
解法：先估算广播表内存(行数 × 行宽)，超阈值别硬 hint；裁剪列、调大 Mapper 内存。

坑 7：向量化开了但没效果
结论：开了 hive.vectorized.execution.enabled 但查询速度没变化。
原因：表是 TextFile 或查询含不支持的 UDF/复杂类型，自动退化为逐行执行。
解法：确认表用 ORC/Parquet，EXPLAIN 查看计划是否带 vectorized 标记。
```

## 相关文档

- [[08.1-Hive 架构与表管理]] — Hive 架构、Metastore、分区分桶表设计
- [[05.2-SQL]] — SQL 基础语法
- [[05.3-高级SQL]] — 递归 CTE、GROUPING SETS 等进阶 SQL 语法
- [[31.2-SQL 优化]] — 执行计划分析与通用 SQL 性能调优
- [[13.1-数仓建模与分层]] — 数仓分层，Hive 表设计的上游规范
- [[13.3-ETL 与数仓架构]] — Hive 在数仓 ETL 中的位置
- [[06.1-文件格式]] — ORC/Parquet 列式格式与存储优化
- [[07.5-Hadoop 生态]] — Hive 在 Hadoop 生态中的位置
